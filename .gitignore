# Secrets — never commit these
.env
client_secret.json
yt_token.json

# Generated output — large video files
output/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# State is committed (small JSON files — needed for dedup across CI runs)
# state/ is intentionally NOT gitignored — push it so the cache persists

# OS
.DS_Store
Thumbs.db
